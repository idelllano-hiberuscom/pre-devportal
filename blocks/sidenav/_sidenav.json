{
  "definitions": [
    {
      "title": "Section Navigation",
      "id": "sidenav",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "_comment": "name se slugifica y es la clase CSS que entrega AEM, asi que tiene que coincidir con el nombre de la carpeta del bloque. `Section Navigation` producia class=section-navigation y el CSS no se aplicaba.",
              "name": "Sidenav",
              "model": "sidenav"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "sidenav",
      "fields": [
        {
          "component": "text",
          "name": "searchLabel",
          "label": "Search placeholder",
          "description": "Text shown inside the search box. Defaults to Buscar.",
          "valueType": "string"
        },
        {
          "component": "richtext",
          "name": "nav",
          "label": "Navigation",
          "description": "Nested list of links. A list item that contains its own list becomes a group: a heading at the first level and an expandable item below that.",
          "valueType": "string",
          "required": true
        }
      ]
    }
  ],
  "filters": []
}
